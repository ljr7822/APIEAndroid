<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Base.Theme.Xiaoxun" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 状态栏透明，避免冷启时显示颜色 -->
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.Xiaoxun" parent="Base.Theme.Xiaoxun" />

    <style name="APieBottomSheetAnimation" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="android:windowEnterAnimation">@anim/apie_dilaog_bottom_sheet_slide_in</item>
        <item name="android:windowExitAnimation">@anim/apie_dilaog_bottom_sheet_slide_out</item>
    </style>

    <!-- 普通对话框样式 -->
    <style name="ru_permission_dialog_style" parent="@android:style/Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <!-- 开关样式-->
    <style name="apie_switch_btn_view_style">
        <item name="colorControlActivated">@color/apieTheme_colorRed</item>
        <item name="colorSwitchThumbNormal">@color/apieTheme_colorGrayLevel4</item>
        <item name="android:colorForeground">@color/apieTheme_colorGrayLevel4</item>
    </style>

    <style name="APieLoadingDialog" parent="@android:style/Theme.Dialog">
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:background">@android:color/transparent</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>

</resources>