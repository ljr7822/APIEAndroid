<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 背景颜色 -->
    <solid android:color="@color/apieTheme_colorTransparent" />

    <!-- 圆角半径 -->
    <corners android:radius="@dimen/apie_theme_dimension_12" />

    <!-- 边框 -->
    <stroke
        android:width="1dp"
        android:color="@color/apieTheme_colorBlack_alpha_30" />
</shape>
